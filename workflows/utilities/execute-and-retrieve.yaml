# =============================================================================
# Workflow: Execute and Retrieve
# Category: utilities
#
# Executes a shell command on an endpoint via Elastic Defend, waits for
# completion, and retrieves the result. Uses explicit HTTP auth for
# subagent chain reliability.
#
# NOTE: endpoint_ids must be an array. This workflow accepts a single
# endpoint_id (string) and wraps it in a proper YAML array.
#
# Author: Security Agent Mesh (based on Elastic template)
# =============================================================================
name: Execute and Retrieve
description: Execute a command on an endpoint and retrieve the result.
enabled: true

tags:
  - agent-mesh
  - utilities
  - response-actions

triggers:
  - type: manual

inputs:
  - name: endpoint_id
    type: string
    description: "The endpoint ID to execute the command on"
    required: true
  - name: comment
    default: "Command executed via security mesh"
    type: string
    description: "Reason for executing this command"
  - name: command
    type: string
    description: "The command to execute on the endpoint"
    required: true

steps:

  - name: execute_action
    type: http
    on-failure:
      continue: true
    with:
      method: POST
      url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/execute"
      headers:
        Content-Type: application/json
        Authorization: "ApiKey __KIBANA_API_KEY__"
        kbn-xsrf: "true"
        elastic-api-version: "2023-10-31"
      body:
        endpoint_ids:
          - "{{ inputs.endpoint_id }}"
        comment: "{{ inputs.comment }}"
        parameters:
          command: "{{ inputs.command }}"
          timeout: 600

  # ── Poll for completion with escalating backoff ─────────────────────────
  # Checks at 5s, 15s, 30s, 45s, 60s. Short-circuits as soon as the command completes.

  - name: wait_1
    type: wait
    with:
      duration: 5s

  - name: check_1
    type: http
    on-failure:
      continue: true
    with:
      method: GET
      url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/{{ steps.execute_action.output.data.data.id }}"
      headers:
        Authorization: "ApiKey __KIBANA_API_KEY__"
        kbn-xsrf: "true"
        elastic-api-version: "2023-10-31"

  - name: poll_if_pending_1
    type: if
    condition: 'steps.check_1.output.data.data.isCompleted: "false"'
    steps:

      - name: wait_2
        type: wait
        with:
          duration: 10s

      - name: check_2
        type: http
        on-failure:
          continue: true
        with:
          method: GET
          url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/{{ steps.execute_action.output.data.data.id }}"
          headers:
            Authorization: "ApiKey __KIBANA_API_KEY__"
            kbn-xsrf: "true"
            elastic-api-version: "2023-10-31"

      - name: poll_if_pending_2
        type: if
        condition: 'steps.check_2.output.data.data.isCompleted: "false"'
        steps:

          - name: wait_3
            type: wait
            with:
              duration: 15s

          - name: check_3
            type: http
            on-failure:
              continue: true
            with:
              method: GET
              url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/{{ steps.execute_action.output.data.data.id }}"
              headers:
                Authorization: "ApiKey __KIBANA_API_KEY__"
                kbn-xsrf: "true"
                elastic-api-version: "2023-10-31"

          - name: poll_if_pending_3
            type: if
            condition: 'steps.check_3.output.data.data.isCompleted: "false"'
            steps:

              - name: wait_4
                type: wait
                with:
                  duration: 15s

              - name: check_4
                type: http
                on-failure:
                  continue: true
                with:
                  method: GET
                  url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/{{ steps.execute_action.output.data.data.id }}"
                  headers:
                    Authorization: "ApiKey __KIBANA_API_KEY__"
                    kbn-xsrf: "true"
                    elastic-api-version: "2023-10-31"

              - name: poll_if_pending_4
                type: if
                condition: 'steps.check_4.output.data.data.isCompleted: "false"'
                steps:

                  - name: wait_5
                    type: wait
                    with:
                      duration: 15s

  # ── Final status check (always runs) ────────────────────────────────────
  - name: get_action_status
    type: http
    on-failure:
      continue: true
    with:
      method: GET
      url: "__KIBANA_URL__/s/__KIBANA_SPACE__/api/endpoint/action/{{ steps.execute_action.output.data.data.id }}"
      headers:
        Authorization: "ApiKey __KIBANA_API_KEY__"
        kbn-xsrf: "true"
        elastic-api-version: "2023-10-31"

  - name: output_result
    type: console
    with:
      message: |
        Action ID: {{ steps.execute_action.output.data.data.id }}
        Command: {{ inputs.command }}
        Host: {{ steps.get_action_status.output.data.data.hosts[inputs.endpoint_id].name }}
        Completed: {{ steps.get_action_status.output.data.data.isCompleted }}
        Successful: {{ steps.get_action_status.output.data.data.wasSuccessful }}
        Exit Code: {{ steps.get_action_status.output.data.data.outputs[inputs.endpoint_id].content.shell_code }}
        Shell: {{ steps.get_action_status.output.data.data.outputs[inputs.endpoint_id].content.shell }}

        === STDOUT ===
        {{ steps.get_action_status.output.data.data.outputs[inputs.endpoint_id].content.stdout }}

        === STDERR ===
        {{ steps.get_action_status.output.data.data.outputs[inputs.endpoint_id].content.stderr }}
